# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'moneful' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for moneful

  target 'monefulTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'monefulUITests' do
    inherit! :search_paths
    # Pods for testing
  end

pod 'RealmSwift'

pod 'EAIntroView'

pod 'BEMCheckBox'



end
